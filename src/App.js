import React from "react";
import Router from "./routes/router";

const App = () => {
  return (
    <div style={app}>
      <Router />
    </div>
  );
};
export default App;

const app = {
  // position: 'relative'
};
